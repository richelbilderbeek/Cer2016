---
title: "display dataset parameters"
author: "Jolien Gay"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Vignette Title}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

## Code Info

## Example

```{r}
pcname <- system('uname -n', intern = TRUE) 
# User    | PC name 
# --------+-------------------
# Jolien  | ?Aline
# Richel  | fwn-biol-132-102"

if (pcname == "Aline") {
  setwd("c:/Users/Aline/Peregrine")
  getwd()
  # Put all .RDA files in a variable
  all_parameters <- Sys.glob("*.RDa") # alternative, but longer: all_parameters <- list.files(pattern = "\\.RDa$", ignore.case=TRUE)
  print(all_parameters)
  
  file.exists(all_parameters)
  
  show_parameter_files(all_parameters)
}

if (pcname == "fwn-biol-132-102") {
  
  # Final goal
  all_parameter_filenames <- list.files("/home/p230198/Peregrine", pattern = "\\.RDa")
  all_parameter_filenames_full_path <- paste("/home/p230198/Peregrine/", all_parameter_filenames, sep = "")
  Cer2016::show_parameter_files(all_parameter_filenames_full_path)
}
```

```{r}
#---------------------Example---------------------------------
#filename <- filenames[1]
#filename <- "d:/Peregrine/article_0_0_0_0_0.RDa"
#filename <- "toy_example_1.RDa"
#readRDS(filename)
#file.exists(filename)
#file <- read_file(filename)
#names(file)

```